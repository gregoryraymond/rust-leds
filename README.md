# Rust template for ESP32

1. Install binstall (or you can build it all from scratch if you want)
2. cargo binstall espup
3. espup install
4. .cargo/config.toml & rust-toolchain.toml may need to be changed to support your MCU or architecture 


TODO: This is currently giving me an error on windows, this may need to be run w/ WSL for windows.